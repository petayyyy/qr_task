## Запуск без ROS (балванка)  
При поломке дрона или узлов можете запустить режим без подлючения к Ros. Для этого установите зависимости для Python.  
```bash
pip install flask
```
Откройте папку с урезанной версией программы.  
```bash
cd no_ros
```
Запусите веб сервер.  
```bash
python3 main_no_ros.py
```  
Теперь по аддресу http://localhost:5000/ запущена система оценивания задания по мониторингу Qr.
## Интеграция в clover.launch  
Откройте clover.launch и модифицируйте его добавив в конце.  
```bash  
<!-- Qr task -->
<include file="$(find qr_checker)/launch/qr_checker.launch">
    <arg name="simulator" value="$(arg simulator)"/>
    <arg name="led" value="$(arg led)"/>
</include>
```  
После данной модификации перезустите дрон и при повторных включениях вместе с зависимостями дрона будет запускаться система оценивания задания по мониторингу Qr.
## Запуск launch файла  
Для запуска необходимо скопировать пакет в рабочию дирректорию Ros.  
```bash
cp qr_checker ~/catkin_ws/src/
```  
Или скачать пакет с git.  
```bash
cd ~/catkin_ws/src/
git clone https://github.com/petayyyy/qr_checker.git
```
Собрать пакет.  
```bash
cd ~/catkin_ws
catkin_make
```
Запустить непосрественно сам пакет.  
```bash
roslaunch qr_checker qr_checker.launch
```
## Параметры системы:  
Данный пакет можно подстроить под себя с помощью параметров системы. Далее подробнее по каждому:  
### 🔢 `max_qrs` - Количество QR-кодов
- **Тип**: `integer`
- **По умолчанию**: `5`
- **Диапазон**: `1-20` (рекомендуется)
- **Описание**: Максимальное количество уникальных QR-кодов, которые необходимо найти для завершения попытки
- **Влияние на систему**:
  - Определяет количество отображаемых статусов на веб-интерфейсе
  - Влияет на структуру CSV файла с результатами
- **Пример использования**:
  ```bash
  # Для короткой сессии
  roslaunch qr_detector qr_detector.launch max_qrs:=3
  
  # Для расширенной сессии
  roslaunch qr_detector qr_detector.launch max_qrs:=10
  ```
### 🎨 `led` - Световые уведомления
- **Тип**: `boolean` (`true`/`false`)
- **По умолчанию**: `true`
- **Описание**: Включает или отключает LED-индикацию (если доступна)
- **Влияние на систему**:
  - `true`: Включает индикацию звуки при обнаружении QR-кодов
  - `false`: Отключает индикацию
- **Пример использования**:
  ```bash
  # С индикацией (по умолчанию)
  roslaunch qr_detector qr_detector.launch led:=true
  
  # Тихий режим (для тестирования)
  roslaunch qr_detector qr_detector.launch led:=false
  ```
### 📷 `camera_topic` - Топик камеры для захвата изображений  
- **Тип**: `string`  
- **По умолчанию**: `main_camera/image_raw`  
- **Описание**: Указывает ROS-топик, из которого система получает изображения для детектирования QR-кодов.  
- **Влияние на систему**:  
  - Определяет источник видео для анализа.  
  - Позволяет использовать разные камеры или симуляционные источники.  
  - Должен соответствовать топику с типом сообщения `sensor_msgs/Image`.  
- **Пример использования**:  
  ```bash  
  # Использование камеры по умолчанию  
  roslaunch qr_detector qr_detector.launch camera_topic:="main_camera/image_raw"  

  # Использование изобращения с камеры с уменьшенной частотой   
  roslaunch qr_detector qr_detector.launch camera_topic:="/main_camera/image_raw_throttled"  
  ```  
### 🌐 `port` - Порт веб-сервера
- **Тип**: `integer`
- **По умолчанию**: `5000`
- **Диапазон**: `1024-65535`
- **Описание**: Порт, на котором запускается Flask веб-сервер с интерфейсом управления
- **Влияние на систему**:
  - Определяет URL для доступа к веб-интерфейсу
  - Должен быть свободным портом на системе
- **Пример использования**:
  ```bash
  # Стандартный порт
  roslaunch qr_detector qr_detector.launch port:=5000
  
  # Альтернативный порт (если 5000 занят)
  roslaunch qr_detector qr_detector.launch port:=8080
  ```
### 👤 `isNeedName` - Имя участника
- **Тип**: `boolean` (`true`/`false`)
- **По умолчанию**: `true`
- **Описание**: Определяет, требуется ли ввод имени участника перед началом попытки
- **Влияние на систему**:
  - `true`: Показывает поле ввода имени, требует его заполнения
  - `false`: Скрывает поле ввода, сохраняет пустое имя участника в CSV
- **Пример использования**:
  ```bash
  # С запросом имени (для соревнований)
  roslaunch qr_detector qr_detector.launch isNeedName:=true
  
  # Без имени (для тестирования/демо)
  roslaunch qr_detector qr_detector.launch isNeedName:=false
  ```
## 🌐 Доступ к веб-интерфейсу
После запуска система доступна по следующим адресам:

- **Локальный доступ**: `http://localhost:5000`
- **Сетевой доступ**: `http://[IP-адрес]:5000`

Где `[IP-адрес]` - IP вашего компьютера в сети Wi-Fi.
## Доп команды:
### 📅 Установка времени
Установка системного времени и даты для корректной записи данных в таблицу.  
```bash
sudo date --set "23 Sep 2025 15:43:00"
```
### 📁 Подключение USB флеш-накопителя
Первым делом нам нужно узнать, как система идентифицирует вашу флешку. Подключите флешку к Raspberry Pi и выполните команду:
```bash
sudo fdisk -l
```
Эта команда выведет список всех подключенных дисков. **Внимательно изучите вывод.** Ваша флешка будет скорее всего обозначена как `/dev/sda1`, `/dev/sdb1` и т.д. Ориентируйтесь на размер диска, чтобы отличить его от основного системного диска (`/dev/mmcblk0p1` или `/dev/mmcblk0p2`).
**Пример вывода:**
```
Device       Start      End  Sectors  Size Type
/dev/sda1    2048 15667199 15665152  7.5G Microsoft basic data
```
Здесь флешка видна как `/dev/sda1`.
#### Создание точки монтирования
Нам нужно создать папку, которая будет служить "входной дверью" для доступа к данным на флешке. Обычно такие папки создаются в директории `/mnt/`.
1.  Переходим в корневую директорию (чтобы пути были абсолютными):
    ```bash
    cd /
    ```
2.  Создаем папку для флешки (например, `usb`):
    ```bash
    sudo mkdir /mnt/usb
    ```
3.  Меняем владельца этой папки на вашего пользователя (в примере пользователь `pi`, замените на своего, например, `pi`). Это даст вам права на чтение и запись без постоянного использования `sudo`.
    ```bash
    sudo chown pi:pi /mnt/usb
    ```
#### Определение UUID и типа файловой системы
Для надежности монтирования мы будем использовать не имя устройства (которое может меняться), а **UUID** (уникальный идентификатор). Узнаем его с помощью команды `blkid`.
```bash
sudo blkid
```
Найдите в выводе строку, соответствующую вашей флешке. Она будет содержать UUID и `TYPE="vfat"` (или `ext4`, `ntfs`, `exfat`).

**Пример вывода:**
```
/dev/sda1: UUID="5C81-CFF0" TYPE="vfat" PARTUUID="a89f45b2-01"
```
Запишите значение `UUID` и `TYPE`. Они понадобятся в следующем шаге.

#### Настройка автоматического монтирования

Чтобы флешка подключалась автоматически при загрузке, нужно добавить ее в файл `/etc/fstab` — таблицу файловых систем.

1.  Открываем файл для редактирования в редакторе `nano`:
    ```bash
    sudo nano /etc/fstab
    ```

2.  В конце файла добавляем следующую строку, **подставив свои значения** `UUID` и, если необходимо, `uid`/`gid` (имя пользователя и группы):
    ```bash
    UUID=5C81-CFF0 /mnt/usb vfat auto,nofail,noatime,users,rw,uid=pi,gid=pi 0 0
    ```
    **Пояснение параметров:**
    *   `UUID=5C81-CFF0`: Уникальный идентификатор вашей флешки.
    *   `/mnt/usb`: Путь к точке монтирования.
    *   `vfat`: Тип файловой системы. Укажите ваш полученный ранее.
3.  Сохраняем файл и выходим из редактора:
    *   `Ctrl + X` для выхода.
    *   `Y` для подтверждения сохранения.
    *   `Enter` для сохранения под тем же именем.
#### Проверка и применение настроек
1.  Проверим, видит ли система нашу флешку и точку монтирования с помощью `lsblk`:
    ```bash
    lsblk
    ```
    В выводе вы должны увидеть, что у вашего устройства (например, `sda1`) в столбце `MOUNTPOINT` указан путь `/mnt/usb`.
2.  Теперь применим настройки, смонтировав все устройства, описанные в `fstab`:
    ```bash
    sudo mount -a
    ```
    Если эта команда выполнится без ошибок — значит, вы все сделали правильно!

#### Проверка работоспособности
Перейдите в созданную папку и проверьте, что файлы с флешки доступны:
```bash
cd /mnt/usb
ls -la
```
Вы должны увидеть содержимое вашей USB-флешки. Теперь вы можете перезагрузить Raspberry Pi (`sudo reboot`), и после загрузки флешка будет автоматически подключена и доступна по пути `/mnt/usb`.
